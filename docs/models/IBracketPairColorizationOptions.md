# IBracketPairColorizationOptions
`interface` 

## 参数 Properties

### enabled?
+ 类型: `boolean`  `可选`
+ 默认值: `-`
+ 可选项: `-`
+ 描述: `启用或禁用括号对着色。`

### independentColorPoolPerBracketType?
+ 类型: `boolean`  `可选`
+ 默认值: `-`
+ 可选项: `-`
+ 描述: `每个括号类型使用独立的颜色池。`
